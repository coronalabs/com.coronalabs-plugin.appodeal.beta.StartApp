Sources for the plugin `plugin.appodeal.beta.StartApp`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.StartApp" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
